dbt_class:
  outputs:
    dev:
      dbname: postgres
      host: localhost
      pass: postgres
      port: 5432
      schema: defalut_schema
      threads: 1
      type: postgres
      user: postgres
  target: dev
